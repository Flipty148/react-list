import {Container, Typography} from "@mui/material";

export function NotFound() {
    return (
        <Container>
            <Typography variant='h5' component={'h5'}>Страница не найдена</Typography>
        </Container>
    )
}
